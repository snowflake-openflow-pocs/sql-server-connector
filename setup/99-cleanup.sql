-- Drop service
DROP SERVICE IF EXISTS POC_CDC.PUBLIC.CDC_DEMO_SERVICE;

-- Drop compute pool
DROP COMPUTE POOL IF EXISTS CDC_DEMO_POOL;

-- Drop secret
DROP SECRET IF EXISTS POC_CDC.PUBLIC.SQL_SA_PASSWORD;

-- Drop image repository
DROP IMAGE REPOSITORY IF EXISTS POC_CDC.PUBLIC.IMAGES;

-- Drop warehouse
DROP WAREHOUSE IF EXISTS CDC_DEMO_WH;

-- Drop database (commented out for safety)
-- DROP DATABASE IF EXISTS POC_CDC;